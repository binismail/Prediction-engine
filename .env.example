# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=prediction_engine

# Web3 Configuration
# RPC Provider URL for the target chain (e.g., https://mainnet.base.org or your Infura/Alchemy URL)
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/vnHuZFsUG_fJTJ_iOp7e-

# Private Key for the server wallet used to sign withdrawal requests (EIP-712)
# MUST start with 0x or be the raw hex string. 
# WARNING: Do not commit your real private key to version control!
SIGNER_PRIVATE_KEY=your_private_key_here

# Application Configuration
PORT=3000
NODE_ENV=development
